{
  "version": "0.2.0",
  "configurations": [{
    "type": "node", "request": "launch", "name": "Run expense tracker",
    "program": "${workspaceFolder}/src/server.js",
    "runtimeArgs": ["--env-file-if-exists=.env"],
    "cwd": "${workspaceFolder}", "skipFiles": ["<node_internals>/**"]
  }]
}
